server:
  port: 8080
  servlet:
    context-path: /hw7

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/geekbrains?serverTimezone=UTC
    username: root
    password: 7940588
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    show-sql: false
